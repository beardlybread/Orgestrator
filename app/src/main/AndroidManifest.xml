<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.beardlybread.orgestrator">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".Start"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".sandbox.RawFile" />
        <activity android:name=".sandbox.FindToDo" />
        <activity android:name=".sandbox.TextList" />
        <activity android:name=".sandbox.CheckedList" />
        <activity android:name=".sandbox.DriveAPIQuickstart" />
        <activity android:name=".sandbox.Sandbox" />
        <activity android:name=".sandbox.DriveOrgFolderId" />
        <activity android:name=".sandbox.DriveApiActivity"></activity>
    </application>

</manifest>